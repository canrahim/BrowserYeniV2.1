<?xml version="1.0" encoding="utf-8"?>
<!--
Sekmelerin altındaki minimal arama butonu layout - Modern ve Compact
Referans: Material Design Button Guidelines
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:id="@+id/searchButtonContainer">

    <com.google.android.material.button.MaterialButton
        android:id="@+id/searchButton"
        style="@style/Widget.MaterialComponents.Button"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_gravity="center"
        android:text="ComboBox Arama"
        android:textSize="12sp"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:layout_marginStart="4dp"
        android:layout_marginEnd="4dp"
        app:backgroundTint="@color/buttonPrimary"
        app:iconTint="@color/buttonText"
        app:cornerRadius="6dp"
        android:textColor="@color/buttonText"
        android:elevation="0dp"
        app:elevation="0dp" />

</FrameLayout>
